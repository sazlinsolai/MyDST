<div data-role="header" data-backbtn="false" data-position="inline">


  <h1><%= title.blank? ? "myDST" : "#{title}" %></h1>
<% if user_signed_in? %>
  <% if !location.blank? && location == 'main' %>
    
    <%= link_to "Logout", destroy_user_session_path, :class=>"ui-btn-right", :rel => "external", :confirm => "Are you sure you want to logout?" %>
  <% end %>
<% end %>
</div>